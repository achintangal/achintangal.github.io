# Expiring Linux Accounts

At work, I often create temporary accounts for researchers/students. Rather than manually disabling the account, its better to just set an expiry date. 
The linux command `sage` helps you do this.
